rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Rien n'est accessible directement depuis le navigateur.
    // Seul le SDK Admin (utilisé par les Netlify Functions, avec une clé de
    // service côté serveur) peut lire ou écrire — il ignore ces règles.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
